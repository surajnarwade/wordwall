apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordwall.name" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "wordwall.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "wordwall.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
